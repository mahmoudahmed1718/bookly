import 'package:flutter/material.dart';

class BookViewDetailBody extends StatelessWidget {
  const BookViewDetailBody({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
